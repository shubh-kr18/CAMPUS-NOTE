import { Router } from 'express'; import { protect } from '../middleware/authMiddleware.js'; import { attendance,schedule,clubs,club } from '../controllers/dataController.js'; import { chat, checkAiStatus } from '../controllers/aiController.js'; export const attendanceRouter=Router();attendanceRouter.get('/',protect,attendance);export const scheduleRouter=Router();scheduleRouter.get('/',protect,schedule);export const clubRouter=Router();clubRouter.get('/',protect,clubs);clubRouter.get('/:id',protect,club);export const aiRouter=Router();aiRouter.get('/status',protect,checkAiStatus);aiRouter.post('/chat',protect,chat)
