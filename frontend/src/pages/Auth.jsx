import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { BookOpen } from 'lucide-react'; import { login,register } from '../services/authService'
export function AuthPage({mode}){const isLogin=mode==='login';const [form,setForm]=useState({name:'',email:'',password:'',confirmPassword:'',branch:'Electronics & Communication Engineering',graduationYear:'2028'});const [error,setError]=useState('');const [loading,setLoading]=useState(false);const navigate=useNavigate();const submit=async e=>{e.preventDefault();setError('');if(!isLogin&&form.password!==form.confirmPassword)return setError('Passwords do not match.');setLoading(true);try{const result=await (isLogin?login(form):register(form));localStorage.setItem('campus_token',result.token);localStorage.setItem('campus_user',JSON.stringify(result.user));navigate('/dashboard')}catch(e){setError(e.message)}finally{setLoading(false)}};return <main className="grid min-h-screen bg-[#f8f8f6] md:grid-cols-2"><section className="hidden bg-slate-900 p-12 text-white md:flex md:flex-col"><Link to="/" className="flex items-center gap-2 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-lime-300 text-slate-900"><BookOpen size={18}/></span>Campus Notes</Link><div className="my-auto max-w-md"><p className="text-sm font-semibold uppercase tracking-[.18em] text-lime-300">IIIT Bhopal</p><h1 className="mt-4 text-4xl font-semibold leading-tight">Your campus, thoughtfully organized.</h1><p className="mt-5 leading-7 text-slate-400">A focused space for the notes, classes, and communities that shape your student life.</p></div></section><section className="flex items-center justify-center p-5"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:p-9"><Link to="/" className="mb-10 flex items-center gap-2 font-bold md:hidden"><BookOpen/>Campus Notes</Link><p className="text-sm font-medium text-lime-700">{isLogin?'WELCOME BACK':'GET STARTED'}</p><h2 className="mt-2 text-3xl font-semibold">{isLogin?'Log in to Campus Notes':'Create your account'}</h2><p className="mt-2 text-sm text-slate-500">{isLogin?'Continue your student workspace.':'Join the IIIT Bhopal student community.'}</p>{error&&<p className="mt-5 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<div className="mt-6 space-y-4">{!isLogin&&<label className="block text-sm font-medium">Full name<input required className="field" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label>}<label className="block text-sm font-medium">Email<input required type="email" className="field" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="block text-sm font-medium">Password<input required type="password" minLength="6" className="field" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{!isLogin&&<><label className="block text-sm font-medium">Confirm password<input required type="password" className="field" value={form.confirmPassword} onChange={e=>setForm({...form,confirmPassword:e.target.value})}/></label><label className="block text-sm font-medium">Branch<input required className="field" value={form.branch} onChange={e=>setForm({...form,branch:e.target.value})}/></label><label className="block text-sm font-medium">Graduation year<input required type="number" className="field" value={form.graduationYear} onChange={e=>setForm({...form,graduationYear:e.target.value})}/></label></>}</div><button disabled={loading} className="btn-primary mt-7 w-full">{loading?'Please wait…':isLogin?'Log in':'Create account'}</button><p className="mt-5 text-center text-sm text-slate-500">{isLogin?'New here? ':'Already have an account? '}<Link className="font-semibold text-slate-900 underline" to={isLogin?'/register':'/login'}>{isLogin?'Register':'Log in'}</Link></p></form></section></main>}
