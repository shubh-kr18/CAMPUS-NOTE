# ==============================================================================
# CAMPUSNOTES Frontend Environment Configuration
# ==============================================================================
# Note: React/Vite only exposes variables prefixed with VITE_.
# AI keys (e.g. OPENAI_API_KEY) must NEVER be placed in frontend environment files.
VITE_API_URL=http://localhost:5002/api
